import Back from './backIcon'
import Edit from './editIcon'
import CSSModules from 'react-css-modules'
import styles from './icons.scss'

const BackIcon = CSSModules(Back, styles)
const EditIcon = CSSModules(Edit, styles)

export {BackIcon, EditIcon}
